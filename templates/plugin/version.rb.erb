# -*- encoding: utf-8 -*-
#
<%= license_comment %>

module Busser

  module <%= config[:klass_name] %>

    # Version string for the <%= config[:klass_name] %> Busser runner plugin
    VERSION = "0.1.0.dev"
  end
end
